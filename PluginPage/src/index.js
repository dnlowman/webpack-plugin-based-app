import React from 'react';

const PluginPage = ({ actions, state }) => {
    debugger;

    console.log(actions);
    console.log(state);

    return (
        <p>This is a plugin page!</p>
    );
};

export default PluginPage;
